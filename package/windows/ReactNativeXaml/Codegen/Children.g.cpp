#include "pch.h"
#include "XamlMetadata.h"
#include <winrt/Windows.UI.Xaml.Controls.h>
#include <winrt/Windows.UI.Xaml.Controls.Primitives.h>
#include <winrt/Windows.UI.Xaml.Documents.h>
#include <winrt/Windows.UI.Xaml.Shapes.h>
#include <winrt/Windows.UI.Xaml.Controls.Maps.h>
#include <winrt/Windows.UI.Xaml.h>

/*************************************************************
THIS FILE WAS AUTOMATICALLY GENERATED, DO NOT MODIFY MANUALLY
**************************************************************/

  void /*XamlViewManager::*/AddView(xaml::FrameworkElement parent, xaml::UIElement child, int64_t _index) {
    if (auto parentInkToolbarCustomPenButton = parent.try_as<winrt::Windows::UI::Xaml::Controls::InkToolbarCustomPenButton>()) {
      // ConfigurationContent (UIElement, a projected item property)
    }
    if (auto parentInkToolbarPenButton = parent.try_as<winrt::Windows::UI::Xaml::Controls::InkToolbarPenButton>()) {
      // Palette (IList`1, a projected item property)
    }
    if (auto parentInkToolbarCustomToolButton = parent.try_as<winrt::Windows::UI::Xaml::Controls::InkToolbarCustomToolButton>()) {
      // ConfigurationContent (UIElement, a projected item property)
    }
    if (auto parentNavigationViewItem = parent.try_as<winrt::Windows::UI::Xaml::Controls::NavigationViewItem>()) {
      // Icon (IconElement, a projected item property)
    }
    if (auto parentAppBarToggleButton = parent.try_as<winrt::Windows::UI::Xaml::Controls::AppBarToggleButton>()) {
      // Icon (IconElement, a projected item property)
    }
    if (auto parentAppBarButton = parent.try_as<winrt::Windows::UI::Xaml::Controls::AppBarButton>()) {
      // Icon (IconElement, a projected item property)
    }
    if (auto parentListViewBase = parent.try_as<winrt::Windows::UI::Xaml::Controls::ListViewBase>()) {
      // SelectedItems (IList`1, a collection of Object)
      // SelectedRanges (IReadOnlyList`1, a projected item property)
      // SemanticZoomOwner (SemanticZoom, a projected item property)
    }
    if (auto parentListBox = parent.try_as<winrt::Windows::UI::Xaml::Controls::ListBox>()) {
      // SelectedItems (IList`1, a collection of Object)
    }
    if (auto parentCommandBar = parent.try_as<winrt::Windows::UI::Xaml::Controls::CommandBar>()) {
      // PrimaryCommands (IObservableVector`1, a collection of ICommandBarElement)
      // SecondaryCommands (IObservableVector`1, a collection of ICommandBarElement)
    }
    if (auto parentButton = parent.try_as<winrt::Windows::UI::Xaml::Controls::Button>()) {
      // Flyout (FlyoutBase, a projected item property)
    }
    if (auto parentToolTip = parent.try_as<winrt::Windows::UI::Xaml::Controls::ToolTip>()) {
      // PlacementTarget (UIElement, a projected item property)
    }
    if (auto parentSplitButton = parent.try_as<winrt::Windows::UI::Xaml::Controls::SplitButton>()) {
      // Flyout (FlyoutBase, a projected item property)
    }
    if (auto parentSlider = parent.try_as<winrt::Windows::UI::Xaml::Controls::Slider>()) {
      // ThumbToolTipValueConverter (IValueConverter, a projected item property)
    }
    if (auto parentScrollViewer = parent.try_as<winrt::Windows::UI::Xaml::Controls::ScrollViewer>()) {
      // ZoomSnapPoints (IList`1, a projected item property)
    }
    if (auto parentRefreshContainer = parent.try_as<winrt::Windows::UI::Xaml::Controls::RefreshContainer>()) {
      // Visualizer (RefreshVisualizer, a projected item property)
    }
    if (auto parentPage = parent.try_as<winrt::Windows::UI::Xaml::Controls::Page>()) {
      // Frame (Frame, a projected item property)
    }
    if (auto parentNavigationViewItemPresenter = parent.try_as<winrt::Windows::UI::Xaml::Controls::Primitives::NavigationViewItemPresenter>()) {
      // Icon (IconElement, a projected item property)
    }
    if (auto parentNavigationView = parent.try_as<winrt::Windows::UI::Xaml::Controls::NavigationView>()) {
      // MenuItems (IList`1, a collection of Object)
      // AutoSuggestBox (AutoSuggestBox, a projected item property)
    }
    if (auto parentMenuFlyoutSubItem = parent.try_as<winrt::Windows::UI::Xaml::Controls::MenuFlyoutSubItem>()) {
      // Items (IList`1, a collection of MenuFlyoutItemBase)
      // Icon (IconElement, a projected item property)
    }
    if (auto parentMenuFlyoutItem = parent.try_as<winrt::Windows::UI::Xaml::Controls::MenuFlyoutItem>()) {
      // Text (String, content property)
      // Icon (IconElement, a projected item property)
    }
    if (auto parentAutoSuggestBox = parent.try_as<winrt::Windows::UI::Xaml::Controls::AutoSuggestBox>()) {
      // QueryIcon (IconElement, a projected item property)
    }
    if (auto parentUserControl = parent.try_as<winrt::Windows::UI::Xaml::Controls::UserControl>()) {
      // Content (UIElement, content property)
    }
    if (auto parentToggleSwitch = parent.try_as<winrt::Windows::UI::Xaml::Controls::ToggleSwitch>()) {
      // Header (Object, content property)
    }
    if (auto parentTimePicker = parent.try_as<winrt::Windows::UI::Xaml::Controls::TimePicker>()) {
      // Header (Object, content property)
    }
    if (auto parentTextBox = parent.try_as<winrt::Windows::UI::Xaml::Controls::TextBox>()) {
      // HandwritingView (HandwritingView, a projected item property)
    }
    if (auto parentSplitView = parent.try_as<winrt::Windows::UI::Xaml::Controls::SplitView>()) {
      // Content (UIElement, content property)
      // Pane (UIElement, a projected item property)
    }
    if (auto parentSemanticZoom = parent.try_as<winrt::Windows::UI::Xaml::Controls::SemanticZoom>()) {
      // ZoomedInView (ISemanticZoomInformation, content property)
      // ZoomedOutView (ISemanticZoomInformation, a projected item property)
    }
    if (auto parentRichEditBox = parent.try_as<winrt::Windows::UI::Xaml::Controls::RichEditBox>()) {
      // Document (ITextDocument, a projected item property)
      // HandwritingView (HandwritingView, a projected item property)
    }
    if (auto parentRefreshVisualizer = parent.try_as<winrt::Windows::UI::Xaml::Controls::RefreshVisualizer>()) {
      // Content (UIElement, a projected item property)
    }
    if (auto parentPasswordBox = parent.try_as<winrt::Windows::UI::Xaml::Controls::PasswordBox>()) {
      // SelectionFlyout (FlyoutBase, a projected item property)
    }
    if (auto parentMenuBarItem = parent.try_as<winrt::Windows::UI::Xaml::Controls::MenuBarItem>()) {
      // Items (IList`1, a collection of MenuFlyoutItemBase)
    }
    if (auto parentMenuBar = parent.try_as<winrt::Windows::UI::Xaml::Controls::MenuBar>()) {
      // Items (IList`1, a collection of MenuBarItem)
    }
    if (auto parentMediaPlayerElement = parent.try_as<winrt::Windows::UI::Xaml::Controls::MediaPlayerElement>()) {
      // TransportControls (MediaTransportControls, a projected item property)
      // Source (IMediaPlaybackSource, a projected item property)
    }
    if (auto parentMapControl = parent.try_as<winrt::Windows::UI::Xaml::Controls::Maps::MapControl>()) {
      // Children (IList`1, a collection of DependencyObject)
    }
    if (auto parentLoopingSelector = parent.try_as<winrt::Windows::UI::Xaml::Controls::Primitives::LoopingSelector>()) {
      // Items (IList`1, a collection of Object)
    }
    if (auto parentItemsControl = parent.try_as<winrt::Windows::UI::Xaml::Controls::ItemsControl>()) {
      // Items (ItemCollection, a collection of Object)
      // GroupStyle (IObservableVector`1, a projected item property)
      // ItemsPanelRoot (Panel, a projected item property)
    }
    if (auto parentInkToolbarPenConfigurationControl = parent.try_as<winrt::Windows::UI::Xaml::Controls::InkToolbarPenConfigurationControl>()) {
      // PenButton (InkToolbarPenButton, a projected item property)
    }
    if (auto parentInkToolbar = parent.try_as<winrt::Windows::UI::Xaml::Controls::InkToolbar>()) {
      // Children (DependencyObjectCollection, a collection of DependencyObject)
      // TargetInkCanvas (InkCanvas, a projected item property)
      // ActiveTool (InkToolbarToolButton, a projected item property)
    }
    if (auto parentIconSourceElement = parent.try_as<winrt::Windows::UI::Xaml::Controls::IconSourceElement>()) {
      // IconSource (IconSource, content property)
    }
    if (auto parentHubSection = parent.try_as<winrt::Windows::UI::Xaml::Controls::HubSection>()) {
      // ContentTemplate (DataTemplate, content property)
    }
    if (auto parentHub = parent.try_as<winrt::Windows::UI::Xaml::Controls::Hub>()) {
      // SectionHeaders (IObservableVector`1, a collection of Object)
      // Sections (IList`1, a collection of HubSection)
      // SectionsInView (IList`1, a collection of HubSection)
      // SemanticZoomOwner (SemanticZoom, a projected item property)
    }
    if (auto parentHandwritingView = parent.try_as<winrt::Windows::UI::Xaml::Controls::HandwritingView>()) {
      // PlacementTarget (UIElement, a projected item property)
    }
    if (auto parentDatePicker = parent.try_as<winrt::Windows::UI::Xaml::Controls::DatePicker>()) {
      // Header (Object, content property)
    }
    if (auto parentContentControl = parent.try_as<winrt::Windows::UI::Xaml::Controls::ContentControl>()) {
      // Content (Object, content property)
      // ContentTemplateRoot (UIElement, a projected item property)
    }
    if (auto parentCalendarView = parent.try_as<winrt::Windows::UI::Xaml::Controls::CalendarView>()) {
      // SelectedDates (IList`1, a projected item property)
    }
    if (auto parentViewbox = parent.try_as<winrt::Windows::UI::Xaml::Controls::Viewbox>()) {
      // Child (UIElement, content property)
    }
    if (auto parentTextBlock = parent.try_as<winrt::Windows::UI::Xaml::Controls::TextBlock>()) {
      // Inlines (InlineCollection, a collection of Inline)
      // TextHighlighters (IList`1, a projected item property)
      // SelectionFlyout (FlyoutBase, a projected item property)
    }
    if (auto parentSpan = parent.try_as<winrt::Windows::UI::Xaml::Documents::Span>()) {
      // Inlines (InlineCollection, a collection of Inline)
    }
    if (auto parentRun = parent.try_as<winrt::Windows::UI::Xaml::Documents::Run>()) {
      // Text (String, content property)
    }
    if (auto parentRichTextBlockOverflow = parent.try_as<winrt::Windows::UI::Xaml::Controls::RichTextBlockOverflow>()) {
      // OverflowContentTarget (RichTextBlockOverflow, a projected item property)
      // ContentSource (RichTextBlock, a projected item property)
    }
    if (auto parentRichTextBlock = parent.try_as<winrt::Windows::UI::Xaml::Controls::RichTextBlock>()) {
      // Blocks (BlockCollection, a collection of Block)
      // OverflowContentTarget (RichTextBlockOverflow, a projected item property)
      // TextHighlighters (IList`1, a projected item property)
      // SelectionFlyout (FlyoutBase, a projected item property)
    }
    if (auto parentPopup = parent.try_as<winrt::Windows::UI::Xaml::Controls::Primitives::Popup>()) {
      // Child (UIElement, content property)
    }
    if (auto parentPickerFlyout = parent.try_as<winrt::Windows::UI::Xaml::Controls::PickerFlyout>()) {
      // Content (UIElement, content property)
    }
    if (auto parentParallaxView = parent.try_as<winrt::Windows::UI::Xaml::Controls::ParallaxView>()) {
      // Child (UIElement, content property)
      // Source (UIElement, a projected item property)
    }
    if (auto parentParagraph = parent.try_as<winrt::Windows::UI::Xaml::Documents::Paragraph>()) {
      // Inlines (InlineCollection, a collection of Inline)
    }
    if (auto parentPanel = parent.try_as<winrt::Windows::UI::Xaml::Controls::Panel>()) {
      // Children (UIElementCollection, a collection of UIElement)
    }
    if (auto parentMediaElement = parent.try_as<winrt::Windows::UI::Xaml::Controls::MediaElement>()) {
      // TransportControls (MediaTransportControls, a projected item property)
    }
    if (auto parentListPickerFlyout = parent.try_as<winrt::Windows::UI::Xaml::Controls::ListPickerFlyout>()) {
      // SelectedItems (IList`1, a collection of Object)
    }
    if (auto parentInlineUIContainer = parent.try_as<winrt::Windows::UI::Xaml::Documents::InlineUIContainer>()) {
      // Child (UIElement, content property)
    }
    if (auto parentContentPresenter = parent.try_as<winrt::Windows::UI::Xaml::Controls::ContentPresenter>()) {
      // Content (Object, content property)
    }
    if (auto parentBorder = parent.try_as<winrt::Windows::UI::Xaml::Controls::Border>()) {
      // Child (UIElement, content property)
    }
    if (auto parentMenuFlyout = parent.try_as<winrt::Windows::UI::Xaml::Controls::MenuFlyout>()) {
      // Items (IList`1, a collection of MenuFlyoutItemBase)
    }
    if (auto parentFlyout = parent.try_as<winrt::Windows::UI::Xaml::Controls::Flyout>()) {
      // Content (UIElement, content property)
    }
    if (auto parentCommandBarFlyout = parent.try_as<winrt::Windows::UI::Xaml::Controls::CommandBarFlyout>()) {
      // PrimaryCommands (IObservableVector`1, a collection of ICommandBarElement)
      // SecondaryCommands (IObservableVector`1, a collection of ICommandBarElement)
    }
    if (auto parentUIElement = parent.try_as<winrt::Windows::UI::Xaml::UIElement>()) {
      // PointerCaptures (IReadOnlyList`1, a projected item property)
      // ContextFlyout (FlyoutBase, a projected item property)
    }
    if (auto parentFlyoutBase = parent.try_as<winrt::Windows::UI::Xaml::Controls::Primitives::FlyoutBase>()) {
      // Target (FrameworkElement, a projected item property)
    }
  }